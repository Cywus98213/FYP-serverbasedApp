<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Green button background - Darker professional shade -->

    <!-- Disabled state - Dark gray with visible border -->
    <item android:state_enabled="false">
        <shape android:shape="rectangle">
            <solid android:color="@color/button_disabled"/>
            <corners android:radius="6dp"/>
            <stroke android:width="2dp" android:color="@color/ar_light_gray"/>
        </shape>
    </item>

    <!-- Pressed state - Darker green with bright border -->
    <item android:state_pressed="true" android:state_enabled="true">
        <shape android:shape="rectangle">
            <solid android:color="@color/button_pressed_green"/>
            <corners android:radius="6dp"/>
            <stroke android:width="3dp" android:color="@color/button_hover_green"/>
        </shape>
    </item>

    <!-- Focused state - Normal with bright border -->
    <item android:state_focused="true" android:state_enabled="true">
        <shape android:shape="rectangle">
            <solid android:color="@color/button_enabled_green"/>
            <corners android:radius="6dp"/>
            <stroke android:width="3dp" android:color="@color/button_hover_green"/>
        </shape>
    </item>

    <!-- Hovered state (for touch feedback) -->
    <item android:state_hovered="true" android:state_enabled="true">
        <shape android:shape="rectangle">
            <solid android:color="@color/button_hover_green"/>
            <corners android:radius="6dp"/>
            <stroke android:width="2dp" android:color="@color/ar_white"/>
        </shape>
    </item>

    <!-- Default enabled state - Medium dark green -->
    <item android:state_enabled="true">
        <shape android:shape="rectangle">
            <solid android:color="@color/button_enabled_green"/>
            <corners android:radius="6dp"/>
            <stroke android:width="2dp" android:color="@color/button_hover_green"/>
        </shape>
    </item>
</selector>

